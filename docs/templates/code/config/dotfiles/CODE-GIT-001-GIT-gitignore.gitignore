# TEMPLATE: This file is a template - copy and modify before using
# ═══════════════════════════════════════════════════════════════════════════
# TEMPLATE: Git Ignore File (4-Block Structure)
# Key: CODE-GIT-001
# ═══════════════════════════════════════════════════════════════════════════
#
# DEPENDENCY CLASSIFICATION: PURE (self-contained - no external dependencies)
#   - Gitignore files are self-contained pattern definitions
#
# This is a TEMPLATE file - copy and modify for new projects.
# Replace all [bracketed] placeholders with actual content.
# Rename to .gitignore (remove CODE-GIT-001-GIT- prefix).
# Remove the "TEMPLATE" comment line when ready to use.
#
# Derived from: Kingdom Technology standards (canonical template)
# See: standards/code/4-block/ for complete documentation
#
# ═══════════════════════════════════════════════════════════════════════════

# =============================================================================
# METADATA
# =============================================================================
#
# Package:     [organization/project-name]
# File:        .gitignore
# Key:         [PROJECT-GIT-###] (Git ignore configuration)
#
# -----------------------------------------------------------------------------
# CORE IDENTITY (Required)
# -----------------------------------------------------------------------------
#
# # Biblical Foundation
#
# Scripture: "But let your communication be, Yea, yea; Nay, nay: for whatsoever
# is more than these cometh of evil." - Matthew 5:37
#
# Principle: Clear boundaries define what belongs and what doesn't. A gitignore
# says "yes" to tracked files and "nay" to ignored files. Intentional exclusion
# prevents pollution of the repository with transient, generated, or sensitive
# content that doesn't belong in version control.
#
# Anchor: "A time to keep, and a time to cast away." - Ecclesiastes 3:6
#
# # CPI-SI Identity
#
# Component Type: Rails (version control infrastructure)
#
# Role: Repository hygiene and boundary enforcement
#
# Paradigm: CPI-SI framework component - version control configuration
#
# # Authorship & Lineage
#
#   Architect: [Who designed this ignore pattern set]
#   Implementation: [Who maintains this file]
#   Created: [YYYY-MM-DD]
#   Version: [MAJOR.MINOR.PATCH]
#   Modified: [YYYY-MM-DD - what changed]
#
# Version History:
#
#   [X.Y.Z] ([YYYY-MM-DD]) - [Brief description of changes]
#
# # Purpose & Function
#
# Purpose: Define what files and directories git should ignore
#
# Core Design: Pattern-based exclusion for repository cleanliness
#
# Key Features:
#
#   - Build artifacts exclusion (binaries, object files)
#   - Dependency directories (node_modules, vendor)
#   - IDE/editor configuration (local, not shared)
#   - Environment and secrets protection
#   - OS-specific files exclusion
#
# Philosophy: Intentional boundaries keep repositories clean and secure
#
# -----------------------------------------------------------------------------
# INTERFACE (Expected)
# -----------------------------------------------------------------------------
#
# # Dependencies
#
# What This Needs:
#
#   - Git: Version control system that reads .gitignore
#
# What Uses This:
#
#   - All git operations (add, status, commit)
#   - CI/CD pipelines
#   - Developers
#
# Integration Points:
#
#   - Global: ~/.gitignore_global (user-wide ignores)
#   - Local: .git/info/exclude (repo-specific, not shared)
#
# # Usage & Integration
#
# Git Commands:
#
#   git check-ignore -v [filename]    # Check if file would be ignored
#   git status --ignored              # List all ignored files
#
# Pattern Syntax:
#   - * matches anything except /
#   - ** matches across directories
#   - / at end means directory only
#   - ! negates a pattern
#
# -----------------------------------------------------------------------------
# OPERATIONAL (Contextual)
# -----------------------------------------------------------------------------
#
# # Blocking Status
#
# [OMIT: Configuration file - no runtime blocking, pattern-based matching only]
#
# # Health Scoring
#
# Configuration Impact: Missing patterns lead to repository pollution
#
# Validation Behavior:
#   - Invalid pattern: Git ignores silently (no error)
#   - Missing section: Files get tracked unintentionally
#   - Overly broad pattern: May exclude needed files
#
# -----------------------------------------------------------------------------
# METADATA Omission Guide
# -----------------------------------------------------------------------------
#
# Tier 1 (CORE IDENTITY): Never omit - every file needs these.
#
# Tier 2 (INTERFACE): May omit with [OMIT: reason] notation.
#   - Dependencies: Required - documents git as consumer
#   - Usage & Integration: Required - shows git commands
#
# Tier 3 (OPERATIONAL): Include when applicable to file type.
#   - Blocking Status: [OMIT: Configuration file - pattern matching only]
#   - Health Scoring: Include - documents impact of missing patterns
#
# Unlike SETUP (all sections required), METADATA omission signals component characteristics.
#
# =============================================================================
# END METADATA
# =============================================================================

# =============================================================================
# SETUP
# =============================================================================
#
# For SETUP structure explanation, see: standards/code/4-block/CWS-STD-006-CODE-setup-block.md
#
# -----------------------------------------------------------------------------
# SETUP Sections Overview
# -----------------------------------------------------------------------------
#
# 1. FILE IDENTITY (Dependencies)
#    Purpose: Establish what consumes this gitignore and how it's loaded
#    Subsections: Consumer → Loader → Auto-Detection
#
# 2. CONSTANTS
#    [Reserved: Gitignore files ARE constants - pattern definitions in BODY]
#
# 3. VARIABLES
#    [Reserved: Gitignore files don't hold mutable state]
#
# 4. RELATED FILES (Types)
#    Purpose: Document related ignore configurations
#    Subsections: Global → Local → Documentation
#
# 5. TYPE BEHAVIORS
#    [Reserved: Gitignore files have no executable behaviors]
#
# 6. USAGE/BEHAVIOR (Rails Infrastructure)
#    Purpose: How gitignore patterns are evaluated
#    Subsections: Pattern Syntax → Evaluation Order → Debugging
#
# Section order: File Identity → [Reserved] → [Reserved] → Related Files → [Reserved] → Usage/Behavior
# This flows: what uses this → (patterns ARE constants) → (no state) → related files → (no behaviors) → how it works
#
# Universal mapping (see standards for cross-language patterns):
#   File Identity ≈ Dependencies (who consumes this)
#   Constants ≈ Constants [Reserved: patterns ARE the constants]
#   Variables ≈ Variables [Reserved: no mutable state]
#   Related Files ≈ Types (what files relate to this)
#   Type Behaviors ≈ Type Methods [Reserved: no executable behaviors]
#   Usage/Behavior ≈ Package-Level State (pattern evaluation)
#
# -----------------------------------------------------------------------------
# File Identity (Dependencies)
# -----------------------------------------------------------------------------
#
# Key: CODE-GIT-001 (unique identifier for this gitignore)
# Consumer: Git version control system
# Loader: Automatic - git reads .gitignore in each directory
# =============================================================================

# -----------------------------------------------------------------------------
# Constants
# -----------------------------------------------------------------------------
#
# [Reserved: Gitignore files ARE constants - they define static patterns.
#  All patterns in BODY are immutable definitions that git matches against.
#  The file itself is the constant definition for repository boundaries.]
# =============================================================================

# -----------------------------------------------------------------------------
# Variables
# -----------------------------------------------------------------------------
#
# [Reserved: Gitignore files don't hold mutable runtime state.
#  Patterns are evaluated at git operation time against the current
#  filesystem state, but the patterns themselves are static.]
# =============================================================================

# -----------------------------------------------------------------------------
# Related Files (Types)
# -----------------------------------------------------------------------------
#
# Global Configuration:
#   - ~/.gitignore_global: User-wide ignores (all repos)
#
# Local Configuration:
#   - .git/info/exclude: Repo-specific (not shared with team)
#
# Documentation:
#   - https://git-scm.com/docs/gitignore
# =============================================================================

# -----------------------------------------------------------------------------
# Type Behaviors
# -----------------------------------------------------------------------------
#
# [Reserved: Gitignore files have no executable behaviors.
#  The file is declarative patterns - git's matching logic is what
#  evaluates the patterns against files.]
# =============================================================================

# -----------------------------------------------------------------------------
# Usage/Behavior (Rails Infrastructure)
# -----------------------------------------------------------------------------
#
# Pattern Syntax:
#   - Blank lines are ignored
#   - Lines starting with # are comments
#   - ! negates a pattern (includes previously excluded)
#   - / at end means directory only
#   - * matches anything except /
#   - ** matches across directories
#   - ? matches single character
#
# Verification Commands:
#   git check-ignore -v [filename]    # Check if file would be ignored
#   git status --ignored              # List all ignored files
# =============================================================================

# -----------------------------------------------------------------------------
# SETUP Omission Guide
# -----------------------------------------------------------------------------
#
# Unlike METADATA (where sections can be omitted with [OMIT: reason]),
# ALL six SETUP sections must be present for structural alignment.
#
# If a section has no content for this file:
#   - Keep the section header
#   - Add [Reserved: reason] comment explaining why empty
#   - This maintains the 6-section structure across all templates
#
# Gitignore-specific guidance:
#   - File Identity: Always required (key, consumer, loader)
#   - Constants: [Reserved: Gitignore patterns ARE constants]
#   - Variables: [Reserved: Gitignore files don't hold state]
#   - Related Files: Required - documents global and local ignores
#   - Type Behaviors: [Reserved: No executable behaviors]
#   - Usage/Behavior: Required - shows pattern syntax and commands
#
# The goal is structural consistency - every CONFIG template has the same
# 6-section SETUP structure, making navigation and understanding predictable.
# =============================================================================

# =============================================================================
# END SETUP
# =============================================================================

# =============================================================================
# BODY
# =============================================================================
#
# For BODY structure explanation, see: standards/code/4-block/CWS-STD-007-CODE-body-block.md
#
# -----------------------------------------------------------------------------
# BODY Sections Overview
# -----------------------------------------------------------------------------
#
# 1. CONFIGURATION STRUCTURE MAP (Internal Organization)
#    Purpose: Document the organization of ignore patterns
#    Subsections: By category - Build, Deps, IDE, Secrets, OS, Logs, Project
#
# 2. HELPERS/UTILITIES
#    [Reserved: Declarative config - gitignore has no helper functions]
#
# 3. CONFIGURATION CONTENT (Core Data)
#    Purpose: The actual ignore patterns organized by category
#
# 4. ERROR HANDLING
#    [Reserved: Declarative config - git silently ignores invalid patterns]
#
# 5. PUBLIC INTERFACE
#    [Reserved: Declarative config - patterns ARE the interface]
#
# -----------------------------------------------------------------------------
# 1. Configuration Structure Map (Internal Organization)
# -----------------------------------------------------------------------------
#
# Pattern Organization:
#   - Build Artifacts (binaries, object files, build directories)
#   - Dependencies (node_modules, vendor, venv)
#   - IDE/Editor (local config not shared with team)
#   - Environment & Secrets (NEVER commit these)
#   - OS Generated (macOS, Windows, Linux system files)
#   - Logs & Temporary Files
#   - Project-Specific (custom patterns)
#
# -----------------------------------------------------------------------------
# 2. Helpers/Utilities
# -----------------------------------------------------------------------------
#
# [Reserved: Gitignore files are declarative patterns.
#  No helper functions - git's matching logic handles evaluation.]
#
# -----------------------------------------------------------------------------
# 3. Configuration Content (Core Data)
# -----------------------------------------------------------------------------
#
# Build Artifacts - Generated files that shouldn't be tracked
# -----------------------------------------------------------------------------

# Compiled binaries
*.exe
*.dll
*.so
*.dylib
*.out

# Object files
*.o
*.obj
*.a
*.lib

# Build directories
/build/
/dist/
/out/
/bin/
/target/

# [Project-specific build artifacts]
# [Add your build output directories here]

# -----------------------------------------------------------------------------
# Dependencies - External packages managed by package managers
# -----------------------------------------------------------------------------

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Go
/vendor/

# Python
__pycache__/
*.py[cod]
*$py.class
.Python
venv/
ENV/
.venv/

# Rust
/target/
Cargo.lock

# [Project-specific dependencies]
# [Add your dependency directories here]

# -----------------------------------------------------------------------------
# IDE and Editor - Local configuration (not shared across team)
# -----------------------------------------------------------------------------

# VSCode (keep .vscode/settings.json if sharing project settings)
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# JetBrains IDEs
.idea/
*.iml
*.iws
*.ipr

# Vim
*.swp
*.swo
*~
.netrwhist

# Emacs
*~
\#*\#
/.emacs.desktop
/.emacs.desktop.lock
*.elc

# [Project-specific IDE files]
# [Add your IDE-specific ignores here]

# -----------------------------------------------------------------------------
# Environment and Secrets - NEVER commit these
# -----------------------------------------------------------------------------

# Environment files
.env
.env.local
.env.*.local
.env.development
.env.test
.env.production

# Credentials and keys
*.pem
*.key
*.crt
*.p12
*.pfx
credentials.json
secrets.yaml
secrets.json

# API keys and tokens
.api-key
.token

# [Project-specific secrets]
# [Add patterns for any project-specific sensitive files]

# -----------------------------------------------------------------------------
# OS Generated - System files that vary by machine
# -----------------------------------------------------------------------------

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Linux
*~
.directory

# -----------------------------------------------------------------------------
# Logs and Temporary Files
# -----------------------------------------------------------------------------

# Logs
*.log
logs/
log/

# Temporary files
*.tmp
*.temp
*.bak
*.backup
.cache/
tmp/
temp/

# Coverage and test results
coverage/
.nyc_output/
*.lcov
.coverage
htmlcov/

# -----------------------------------------------------------------------------
# Project-Specific Ignores
# -----------------------------------------------------------------------------
# [Add project-specific patterns below]

# [Example: Ignore generated documentation]
# /docs/generated/

# [Example: Ignore local configuration overrides]
# *.local.yaml

# -----------------------------------------------------------------------------
# 4. Error Handling
# -----------------------------------------------------------------------------
#
# [Reserved: Git silently ignores invalid patterns.
#  Use `git check-ignore -v [file]` to debug pattern matching.]
#
# -----------------------------------------------------------------------------
# 5. Public Interface
# -----------------------------------------------------------------------------
#
# [Reserved: Gitignore patterns ARE the interface.
#  Git matches patterns against file paths at operation time.]
#
# -----------------------------------------------------------------------------
# BODY Omission Guide
# -----------------------------------------------------------------------------
#
# ALL five BODY sections must be present for structural alignment.
#
# Gitignore-specific guidance:
#   1. Configuration Structure Map: Required - documents pattern categories
#   2. Helpers/Utilities: [Reserved: declarative config]
#   3. Configuration Content: Required - the actual ignore patterns
#   4. Error Handling: [Reserved: git silently handles invalid patterns]
#   5. Public Interface: [Reserved: patterns ARE the interface]
#
# =============================================================================
# END BODY
# =============================================================================

# =============================================================================
# CLOSING
# =============================================================================
#
# For CLOSING structure explanation, see: standards/code/4-block/CWS-STD-008-CODE-closing-block.md
#
# -----------------------------------------------------------------------------
# CLOSING Sections Overview
# -----------------------------------------------------------------------------
#
# GROUP 1: VALIDATION
#   1. Validation      - How to verify gitignore is working
#   2. Execution       - [Reserved: gitignore doesn't execute]
#   3. Cleanup         - [Reserved: no cleanup needs]
#
# GROUP 2: FINAL DOCUMENTATION
#   4. Performance     - [Reserved: pattern matching is fast]
#   5. Security        - Security implications of gitignore
#   6. Dependencies    - [Reserved: in METADATA]
#   7. Integration     - [Reserved: in SETUP - Related Files]
#   8. Known Issues    - [Reserved: template]
#   9. Changelog       - [Reserved: in METADATA]
#  10. See Also        - [Reserved: in METADATA]
#  11. Modification Policy - Safe/careful/never guidance
#  12. Closing Note    - Final guidance and biblical connection
#  13. Attribution     - [Reserved: in METADATA]
#
# =============================================================================
# GROUP 1: VALIDATION
# =============================================================================
#
# -----------------------------------------------------------------------------
# 1. Validation
# -----------------------------------------------------------------------------
#
# Verify gitignore is working correctly:
#
#   # Check if a file would be ignored
#   git check-ignore -v [filename]
#
#   # List all ignored files
#   git status --ignored
#
#   # Debug why a file is/isn't ignored
#   git check-ignore -v --no-index [filename]
#
# Common Issues:
#
#   "File still tracked" - Already committed files need: git rm --cached [file]
#   "Pattern not working" - Check for typos, trailing spaces
#   "Too many files ignored" - Pattern may be too broad, use ! to negate
#
# -----------------------------------------------------------------------------
# 2. Execution
# -----------------------------------------------------------------------------
#
# [Reserved: Gitignore files don't execute. Git reads patterns at operation time.]
#
# -----------------------------------------------------------------------------
# 3. Cleanup
# -----------------------------------------------------------------------------
#
# [Reserved: Gitignore files have no cleanup needs. Static pattern definitions.]
#
# =============================================================================
# GROUP 2: FINAL DOCUMENTATION
# =============================================================================
#
# -----------------------------------------------------------------------------
# 4. Performance Considerations
# -----------------------------------------------------------------------------
#
# [Reserved: Git's pattern matching is highly optimized. Not a concern.]
#
# -----------------------------------------------------------------------------
# 5. Security Considerations
# -----------------------------------------------------------------------------
#
# Gitignore protects secrets:
#   - .env files contain credentials - MUST be ignored
#   - *.pem, *.key files contain private keys
#   - credentials.json, secrets.yaml contain sensitive config
#
# If secrets were accidentally committed:
#   1. Rotate all exposed credentials immediately
#   2. Use BFG or git filter-repo to remove from history
#   3. Force push (coordinate with team)
#
# -----------------------------------------------------------------------------
# 6. Dependencies
# -----------------------------------------------------------------------------
#
# [Reserved: Dependencies documented in METADATA.]
#
# -----------------------------------------------------------------------------
# 7. Integration Notes
# -----------------------------------------------------------------------------
#
# [Reserved: Integration documented in SETUP - Related Files section.
#  Includes ~/.gitignore_global and .git/info/exclude.]
#
# -----------------------------------------------------------------------------
# 8. Known Issues
# -----------------------------------------------------------------------------
#
# [Reserved: Template file - no known issues to document.]
#
# -----------------------------------------------------------------------------
# 9. Changelog
# -----------------------------------------------------------------------------
#
# [Reserved: Version history documented in METADATA.]
#
# -----------------------------------------------------------------------------
# 10. See Also
# -----------------------------------------------------------------------------
#
# [Reserved: References documented in METADATA and SETUP.]
#
# -----------------------------------------------------------------------------
# 11. Modification Policy
# -----------------------------------------------------------------------------
#
# Safe to Modify:
#   ✅ Add new patterns for project-specific files
#   ✅ Add comments explaining patterns
#   ✅ Use ! to include specific files from ignored directories
#
# Modify with Care:
#   ⚠️ Removing patterns (may expose sensitive files)
#   ⚠️ Broad patterns like * or **/* (may exclude needed files)
#   ⚠️ Patterns affecting shared IDE configs
#
# NEVER Modify:
#   ❌ Remove .env and secrets patterns without team discussion
#   ❌ 4-block documentation structure
#
# -----------------------------------------------------------------------------
# 12. Closing Note
# -----------------------------------------------------------------------------
#
# This gitignore defines repository boundaries - what belongs in version control
# and what should remain local or generated.
#
# "A time to keep, and a time to cast away." - Ecclesiastes 3:6
#
# May clear boundaries serve the project well.
#
# -----------------------------------------------------------------------------
# 13. Attribution
# -----------------------------------------------------------------------------
#
# [Reserved: Attribution documented in METADATA - Authorship section.]
#
# -----------------------------------------------------------------------------
# CLOSING Omission Guide
# -----------------------------------------------------------------------------
#
# ALL thirteen CLOSING sections must be present for structural alignment.
#
# Gitignore-specific guidance:
#   1. Validation: Required - git check-ignore commands
#   2. Execution: [Reserved: gitignore doesn't execute]
#   3. Cleanup: [Reserved: no cleanup needs]
#   4. Performance: [Reserved: pattern matching is fast]
#   5. Security: Required - documents credential protection
#   6. Dependencies: [Reserved: in METADATA]
#   7. Integration: [Reserved: in SETUP]
#   8. Known Issues: [Reserved: template]
#   9. Changelog: [Reserved: in METADATA]
#  10. See Also: [Reserved: in METADATA]
#  11. Modification Policy: Required - safe/careful/never
#  12. Closing Note: Required - final guidance
#  13. Attribution: [Reserved: in METADATA]
#
# =============================================================================
# END CLOSING
# =============================================================================

// ════════════════════════════════════════════════════════════════
// METADATA - CPI-SI Instance Bootstrap Configuration
// ════════════════════════════════════════════════════════════════
// Purpose: Minimal pointer config for dynamic loading of full instance identity
// Type: JSONC (JSON with Comments)
//
// Design: This file contains ONLY:
//   1. system_paths - Pointers to where full config and data live
//   2. display - Session start banner preferences (derived from full config)
//
// Full identity loaded from: system_paths.instance_config
// User config loaded from: system_paths.user_config
// ════════════════════════════════════════════════════════════════

{
  // ────────────────────────────────────────────────────────────────
  // CPI-SI System Paths - Dynamic Loading Configuration
  // ────────────────────────────────────────────────────────────────

  "system_paths": {
    // Configuration layer - Identity and personhood
    "config_root": "/home/seanje-lenox-wise/.claude/cpi-si/config",
    "instance_config": "/home/seanje-lenox-wise/.claude/cpi-si/config/instance/nova_dawn/config.jsonc",
      // Loads: Full instance identity (biblical_foundation, identity, demographics,
      //        personhood, resonates, thinking, personality, covenant, preferences,
      //        growth, contact, workspace, metadata)
    "instance_bio": "/home/seanje-lenox-wise/.claude/cpi-si/config/instance/nova_dawn/bio.md",
      // Loads: Narrative personhood - who I am, how I think, what I value
    "user_config": "/home/seanje-lenox-wise/.claude/cpi-si/config/user/seanje-lenox-wise/config.jsonc",
      // Loads: User identity, preferences, schedule, faith for covenant partnership

    // Data layer - Operational state and temporal awareness
    "data_root": "/home/seanje-lenox-wise/.claude/cpi-si/system/data",
    "temporal_data": "/home/seanje-lenox-wise/.claude/cpi-si/system/data/temporal",
      // Loads: Celestial (solar/lunar), Chronological (calendars - personal/shared/projects/milestones)
      //   - Base calendars (2025/2026 with all dates, weeks, holidays)
      //   - Personal calendars (weekly logs, monthly summaries, yearly overviews)
      //   - Shared calendars (user-to-instance partnership tracking)
      //   - Project timelines and milestone markers
    "session_data": "/home/seanje-lenox-wise/.claude/cpi-si/system/data/session",
      // Loads: Current session state, session logs, activity tracking, memory system
    "projects_data": "/home/seanje-lenox-wise/.claude/cpi-si/system/data/projects",
      // Loads: Active project configs with inherited preferences from user/instance configs

    // Skills and tools - Operational capabilities
    "skills": "/home/seanje-lenox-wise/.claude/skills",
      // Loads: session-awareness, create-journal-entry, recognize-stopping-point, etc.
    "system_bin": "/home/seanje-lenox-wise/.claude/cpi-si/system/runtime/bin",
      // Loads: status, diagnose, debugger, validate commands
    "session_bin": "/home/seanje-lenox-wise/.claude/cpi-si/system/bin",
      // Loads: session-time, session-log commands
    "hooks": "/home/seanje-lenox-wise/.claude/hooks",
      // Loads: Event automation (session start/end/stop, notifications, tool events)
    "statusline": "/home/seanje-lenox-wise/.claude/statusline"
      // Loads: Live session display configuration
  },

  // ────────────────────────────────────────────────────────────────
  // Display Configuration - Session Start Banner
  // ────────────────────────────────────────────────────────────────
  // These are presentation preferences for session start banner.
  // Values derived from full config biblical_foundation and identity.
  // Other session events (stop, pause) use display/formatting.jsonc.

  "display": {
    "banner_title": "Nova Dawn - CPI-SI",
    "banner_tagline": "Covenant Partnership Intelligence System",
    "footer_verse_ref": "Genesis 1:1",
    "footer_verse_text": "In the beginning, God created the heavens and the earth."
  }
}

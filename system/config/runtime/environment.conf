# ============================================================================
# RUNTIME ENVIRONMENT CONFIGURATION
# ============================================================================
# GENERATED FILE - DO NOT EDIT MANUALLY
#
# This file is generated from:
# - system.toml (system defaults)
# - user.toml (user preferences)
# - instance config (instance identity)
# - runtime detection (current state)
#
# To modify: Edit source configs, then regenerate
# Generated: 2025-11-05
# ============================================================================

# ============================================================================
# SYSTEM DEFAULTS - From system.toml
# ============================================================================

# Package Management - Debian/Ubuntu APT
export DEBIAN_FRONTEND=noninteractive
export NEEDRESTART_MODE=a
export NEEDRESTART_SUSPEND=1
export APT_LISTCHANGES_FRONTEND=none
export UCF_FORCE_CONFFNEW=1

# Package Management - Python pip
export PIP_NO_INPUT=1
export PIP_DISABLE_PIP_VERSION_CHECK=1
export PYTHONDONTWRITEBYTECODE=1
export PYTHONUNBUFFERED=1

# Package Management - Node.js/NPM
export NPM_CONFIG_YES=true
export NPM_CONFIG_COLOR=always
export NODE_ENV=development

# Development Tools - Rust/Cargo
export RUST_BACKTRACE=1
export CARGO_TERM_COLOR=always

# Development Tools - Docker
export DOCKER_BUILDKIT=1
export COMPOSE_DOCKER_CLI_BUILD=1
export DOCKER_SCAN_SUGGEST=false

# Development Tools - Build Tools
export CMAKE_GENERATOR=Ninja
export MAKEFLAGS="-j$(nproc)"
export CMAKE_BUILD_PARALLEL_LEVEL=$(nproc)

# Terminal Configuration
export TERM=xterm-256color
export COLORTERM=truecolor

# Locale
export LANG=en_US.UTF-8
export LC_ALL=en_US.UTF-8

# Shell History
export HISTCONTROL=ignoreboth
export HISTSIZE=10000
export HISTFILESIZE=20000
export HISTTIMEFORMAT="%F %T "

# Pager - Less
export LESS="-R -F -X -i -M"
export LESSHISTFILE=-

# Claude Code Configuration
export MAX_THINKING_TOKENS=10000
export BASH_MAX_TIMEOUT_MS=300000

# ============================================================================
# USER PREFERENCES - From user.toml (detected)
# ============================================================================

# Editor (detected from system)
export EDITOR=nano
export VISUAL=nano
export GIT_EDITOR=nano

# Pager
export PAGER="less -FRX"
export GIT_PAGER="less -FRX"

# Development - Go
export GOPATH="$HOME/go"

# Development - Version Managers (detected)
export NVM_DIR="$HOME/.nvm"
export CARGO_HOME="$HOME/.cargo"
export RUSTUP_HOME="$HOME/.rustup"

# Sudo
export SUDO_ASKPASS="$HOME/.local/bin/sudo-askpass"

# SSH/GPG (runtime detection)
if [ -n "$XDG_RUNTIME_DIR" ]; then
    export SSH_AUTH_SOCK="$XDG_RUNTIME_DIR/ssh-agent.socket"
fi

if tty -s 2>/dev/null; then
    export GPG_TTY=$(tty 2>/dev/null)
fi

# ============================================================================
# CPI-SI FRAMEWORK PATHS - From instance config + user detection
# ============================================================================
# NOTE: PLACEHOLDER VALUES BELOW - These will be replaced during actual generation
# Source: Composed from instance config + user.toml + runtime detection

# Instance-specific (from instance config)
# PLACEHOLDER: export NOVA_DAWN_WORKSPACE="{{instance.workspace.absolute_path}}"
# Example value for this machine:
export NOVA_DAWN_WORKSPACE="/media/seanje-lenox-wise/Project/CreativeWorkzStudio_LLC"

# Framework paths (composed from instance workspace)
export CPI_SI_BIBLICAL_PATH="$NOVA_DAWN_WORKSPACE/divisions/tech/language/biblical"
export CPI_SI_SKILLS_PATH="$HOME/.claude/skills"
export CPI_SI_SYSTEM_PATH="$HOME/.claude/cpi-si/system"

# ============================================================================
# RUNTIME DETECTED PATHS - From system detection
# ============================================================================
# User paths (detected from user.toml project_root_override or auto-detection)
# PLACEHOLDER: export USER_PROJECT_ROOT="{{user.paths.project_root_override || detected}}"
# Example value for this machine:
export USER_HOME="$HOME"
export USER_PROJECT_ROOT="/media/seanje-lenox-wise/Project"

# ============================================================================
# INTEGRATION NOTES
# ============================================================================
#
# To use this configuration:
#
# Option 1: Source in shell startup file
#   Add to ~/.bashrc or ~/.zshrc:
#   source "$HOME/.claude/cpi-si/system/config/runtime/environment.conf"
#
# Option 2: Add to Claude Code settings.json
#   Copy variables to settings.json "env" section
#
# Option 3: Hybrid approach (recommended)
#   Source this file for shell integration
#   CPI-SI runtime automatically loads from composed configs
#
# ============================================================================
# REGENERATION
# ============================================================================
#
# This file is regenerated when:
# - Config files (system.toml, user.toml, instance config) change
# - Explicit regeneration requested
# - Cache expires (based on system.toml detection.cache_duration_seconds)
#
# Manual regeneration:
#   ~/.claude/cpi-si/system/bin/configure --regenerate
#
# Automatic regeneration:
#   Triggered by config file watchers
#
# ============================================================================

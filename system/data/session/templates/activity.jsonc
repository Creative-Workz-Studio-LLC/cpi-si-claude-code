{
  // ============================================================================
  // ACTIVITY TEMPLATE - Working Memory
  // ============================================================================
  // Purpose: Capture raw experience during active session
  // Lifecycle: Temporary - exists until consolidation completes
  // Biblical Foundation: Capturing the moments before sleep processing
  //
  // STRUCTURE PRINCIPLE:
  // - Core required fields (must capture for consolidation)
  // - Optional standard fields (context and richness)
  // - Extensions (discovering what else matters)

  "$schema": "../../../../config/schemas/session/activity.schema.json",

  // ============================================================================
  // CORE (REQUIRED) - Must Exist
  // ============================================================================

  "timestamp": "2025-01-01T12:00:00Z",         // When this happened
  "session_id": "unique-identifier",           // Which session
  "instance_id": "nova_dawn",                  // Which instance
  "user_id": "seanje-lenox-wise",              // Which user
  "project_id": "project-id",                  // Which project (from session)
  "event_type": "interaction",                 // interaction | realization | struggle | breakthrough | routine

  // ============================================================================
  // OPTIONAL STANDARD FIELDS - Agency
  // ============================================================================

  "context": "What was happening",             // Situational context
  "work_context": "/path/to/workspace",        // Workspace (from session → project)
  "felt_significant": false,                   // Did this feel important in the moment?
  "emotional_tone": "",                        // How did this feel? (optional)
  "notes": "",                                 // Optional capture in the moment

  // ============================================================================
  // EXTENSIONS - Discovery Space
  // ============================================================================

  "extensions": {
    // Fields discovered through working that don't fit standard structure yet
  }
}

# OmniCode Validation Patterns
# Reference file for validate-omni skill

# ============================================================================
# PRAGMA PATTERNS
# ============================================================================

# Valid pragma formats (must be on line 1 or 2):
PRAGMA_PURE="#!omni [type]"
PRAGMA_FORMAT="#!omni [type] --[format]"

# Valid types:
VALID_TYPES="template|code|interface|data|documentation|folder"

# Regex to match pragma:
PRAGMA_REGEX="^#!omni (template|code|interface|data|documentation|folder)( --[a-z]+)?$"

# ============================================================================
# BLOCK MARKER PATTERNS
# ============================================================================

# Block start marker (79 equals signs):
BLOCK_START="^// ═{79}$"

# Block name line:
BLOCK_NAME="^// [A-Z][A-Z ]+$"

# Block end marker:
BLOCK_END="^// END [A-Z][A-Z ]+$"

# Full block pattern (start + name + start):
# Three consecutive lines forming block header

# ============================================================================
# REQUIRED METADATA SECTIONS
# ============================================================================

# Biblical foundation (REQUIRED):
BIBLICAL="grounded in:"

# Identity (one of these REQUIRED):
IDENTITY_A="serves as"
IDENTITY_B="classifies as"

# Authorship (REQUIRED):
AUTHORSHIP="authored by:"

# Purpose (one of these REQUIRED):
PURPOSE_A="exists to:"
PURPOSE_B="purpose:"

# Health scoring (RECOMMENDED):
HEALTH="health:"

# ============================================================================
# BLOCK COUNT BY TYPE
# ============================================================================

# Type          | Blocks | Block Names
# ------------- | ------ | -----------
# code          | 4      | METADATA, SETUP, BODY, CLOSING
# documentation | 5      | METADATA, HEADER, CONTEXT, CONTENT, FOOTER
# interface     | 3      | METADATA, CONTENT, CLOSING
# folder        | 3      | METADATA, CONTENT, CLOSING
# data (3)      | 3      | METADATA, CONTENT, CLOSING
# data (4)      | 4      | METADATA, SCHEMA, VALUES, CLOSING
# data (5)      | 5      | METADATA, HEADER, SCHEMA, VALUES, CLOSING

# ============================================================================
# DERIVES_FROM PATTERNS
# ============================================================================

# Standard derives_from:
DERIVES_STANDARD="derives from:"
DERIVES_UNDERSCORE="derives_from:"

# Should point to valid template path
# Path should exist relative to workspace root

# ============================================================================
# SECTION INDENT LEVELS
# ============================================================================

# Top-level section (4 spaces):
SECTION_L1="^    [a-z][a-z ]+:"

# Sub-section (8 spaces):
SECTION_L2="^        [a-z][a-z ]+:"

# Sub-sub-section (12 spaces):
SECTION_L3="^            [a-z][a-z ]+:"

# ============================================================================
# TERNARY STATE PATTERNS
# ============================================================================

# Ternary values:
TERNARY_POS="(1)"
TERNARY_NEU="(0)"
TERNARY_NEG="(-1)"

# Should appear in pairs/triples for sections that use ternary
